---
"@pourkit/cli": minor
---

Automate npm releases with Changesets and prepare CLI for publishing

- `@pourkit/cli` is now published to npm with automated snapshot releases on `next` and stable releases on `main`
- Fix: resolve detached issue worktrees during `--reset-worktree` after a conflicted rebase
