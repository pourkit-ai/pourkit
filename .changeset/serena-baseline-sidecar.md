---
"@pourkit/cli": minor
---

Add optional Serena Batch Baseline integration with `pourkit serena` lifecycle commands, target-scoped baseline refresh, Builder/Refactor MCP injection, and runtime MCP URL overrides.
